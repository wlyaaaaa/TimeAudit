postgres_data/
grafana_data/
log/
*.log
telemetry.log
__pycache__/
*.pyc
time_audit_perfect_archive.sql

# 采集进程 PID 文件：内容随每次重启变化，仅本地信息记录，不进版本库
time_audit.pid

# 本次维护的临时调试脚本/转储，不进版本库
tmp_*.py
tmp_*.txt

# 数据库备份（本地/异地保存，不进版本库）
backups/

# Grafana 二进制库备份(grafana.db 拷贝)=数据，不进 git。
# 仪表盘 JSON=前端代码，在 grafana_dashboards/ 进 git(不在此忽略)。
grafana_backups/

# 临时/调试草稿文件
_live_dash.json
_panels_dump.txt
tmp_dash/
_suite*.txt

time_audit_perfect_archive.sql
