apiVersion: 1
datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: audit-db:5432        # 内部网络物理对齐 docker-compose 中的服务名
    user: leyang
    secureJsonData:
      password: 'SecurePassword123'
    jsonData:
      database: time_audit
      sslmode: disable
      maxOpenConns: 5
      maxIdleConns: 2