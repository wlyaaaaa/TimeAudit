# 🖥️ TimeAudit 使用手册（小白版）

> 一句话：**TimeAudit 是一个 7×24 小时默默记录你这台电脑「身体状况」和「使用情况」的私人黑匣子**——
> 它把 CPU、显卡、内存、硬盘、网络、功耗、每个程序的一举一动都记进数据库，再用 6 张「仪表盘」画成图，
> 让你随时看懂：电脑热不热、卡不卡、费不费电、有没有可疑程序、你每天都在用电脑干什么。

本机配置：**Windows 11 25H2 工作站版 · 锐龙 9950X3D · RTX 5080 · 64GB 内存 · NVMe 固态**。

---

## 目录
1. [它是怎么工作的（30 秒看懂）](#1-它是怎么工作的30-秒看懂)
2. [怎么用：打开仪表盘 + 三个必会操作](#2-怎么用打开仪表盘--三个必会操作)
3. [六大仪表盘逐个讲解（核心）](#3-六大仪表盘逐个讲解核心)
4. [数据库到底记了什么](#4-数据库到底记了什么)
5. [诊断速查：电脑出问题了，看哪张盘？](#5-诊断速查电脑出问题了看哪张盘)
6. [「正常 vs 异常」数字对照表](#6-正常-vs-异常数字对照表)
7. [日常维护、备份、自检](#7-日常维护备份自检)
8. [名词小词典](#8-名词小词典)

---

## 1. 它是怎么工作的（30 秒看懂）

把它想象成一条流水线：

```
 你的电脑                          仓库                  报表
┌──────────────────────┐      ┌───────────┐      ┌──────────────┐
│ 采集器(后台悄悄运行)  │ ──►  │ PostgreSQL │ ──► │   Grafana     │
│  · 硬件探针           │ 写入 │  数据库    │ 读取 │ 6 张仪表盘    │
│  · 进程活动探针       │      │ (装在     │      │ (装在 Docker, │
│  · 前台窗口探针       │      │  Docker)  │      │  网页打开)    │
│  · 进程生灭探针       │      └───────────┘      └──────────────┘
│  · AHK 窗口记录器     │
└──────────────────────┘
```

- **采集器**：一个叫 `main.py` 的 Python 程序，开机自动在后台运行（你看不见窗口）。它每 **3 秒**给电脑「拍一张全身照」，外加实时捕捉每个程序的开关机。
- **数据库（PostgreSQL）**：装在 Docker 里的「仓库」，把所有数据按周/月分箱存好，常年不丢。
- **Grafana**：装在 Docker 里的「报表工具」，用网页打开，把仓库里的数字画成好看的曲线和表格。

> 🔑 **你平时什么都不用管。** 开机它就自己跑起来了。你要做的只有一件事：**想看的时候，用浏览器打开仪表盘。**

---

## 2. 怎么用：打开仪表盘 + 三个必会操作

### ▶ 打开仪表盘
1. 打开浏览器（Chrome / Edge 都行），地址栏输入：**`http://localhost:53000`**
2. 第一次会让登录：用户名 **`admin`**，密码 **`admin`**（本机自用，没改过）。
3. 左上角菜单 → **Dashboards（仪表盘）** → 点你想看的那一张（共 6 张，见下一节）。

### ▶ 三个必会操作（小白只要会这三个就够了）

| 操作 | 在哪 | 干什么用 |
|---|---|---|
| 🕐 **时间范围** | 右上角那个带「时钟」的按钮（如 `Last 1 hour`） | **最重要！** 想看「最近 1 小时」「今天」「最近 7 天」「某一段历史」就点它改。看历史趋势就调长，看实时就调短 |
| 🔄 **自动刷新** | 时间按钮旁边的小圆圈箭头 | 设成 `10s` 就每 10 秒自动更新，适合盯实时；看历史可以关掉 |
| 🖱️ **框选放大** | 在任意曲线图上**按住鼠标左键横向一拖** | 放大看某一小段时间的细节；想还原点左上角的「←」 |

> 💡 鼠标悬停在任何曲线/数字上，会弹出**具体数值和说明**。看不懂某个数？先把鼠标放上去。

---

## 3. 六大仪表盘逐个讲解（核心）

> 每张盘开头我都写了：**「它回答什么问题」+「重点看哪几个图」+「怎么算正常/异常」**。

### 🖥️ 3.1 整机硬件能效与系统资源大盘（默认看最近 2 天）
**回答**：我的 CPU、显卡、内存、硬盘、网络此刻状态如何？热不热？在不在降频？

- **CPU 温度与功率**：温度看红线。9950X3D 平时 **40~55℃ 凉爽**、打游戏/编译 **70~85℃ 正常**、**>90℃ 就在撞「温度墙」**（该清灰/换硅脂了）。
- **CPU 频率与双 CCD 负载（9950X3D 专属）**：这颗 U 有两块「核心区」：
  - **CCD0（大缓存）**= 带 3D 缓存、**打游戏主力**；
  - **CCD1（高频）**= 跑分/多线程主力。
  - 打游戏时如果负载主要压在 CCD1 而不是 CCD0，说明**游戏调度跑错核了**（性能会差）。
- **GPU 温度与功率（RTX5080）**：核心温度 **<78℃ 正常**；**热点温度**通常比核心高 10~15℃，**>95℃ 要警惕**（显存/散热问题）。板卡功率最高可到 ~360W。
- **GPU 降频原因解码时间线（撞了哪堵墙）**：这是一条彩色状态条——
  - 🟢 **满血运行** = 一切正常；🔴 **温度墙** = 太热在降频；🟠 **功耗墙** = 到功耗上限了；🔵 **空闲降频** = 没事在省电（正常）。
- **内存压力与换页压力**：物理内存占用 % + 「系统提交大小(GB)」+「每秒硬缺页」。
  - 占用 **<70% 宽裕**；**提交大小超过你的物理内存(64GB)** 或**硬缺页持续很高** = 内存吃紧、在频繁用硬盘当内存（会变卡）。
- **磁盘最高响应延迟**：**<5ms 健康**，偶尔尖刺正常；**持续 >20ms** = 硬盘在拖后腿。
- **网络质量心电图**：Ping 延迟、抖动 Jitter、丢包率。Ping 低且平、丢包为 0 = 网络好。
- **用户态调度抖动与上下文切换**：可理解为「系统繁忙/被打断的程度」，**越低越平滑**，尖刺多说明系统正被频繁打断。
- **CPU / GPU 散热老化趋势（负载均值）**：长期趋势图。如果**同样的负载下、温度一年比一年高**，就是散热在老化（灰尘/硅脂/风扇）。顶部可调「观测窗口」和「负载判定比例」。

### 🚀 3.2 前台交互与流畅度诊断舱（默认看最近 3 小时）
**回答**：我现在正在用的这个程序/游戏，跟手吗？卡不卡？为什么卡？

- **全谱系流畅度综合线 / 微观手感抖动线**：核心是 **FPS（帧率）**、**1% Low（最差 1% 的帧率）** 和 **帧时间抖动**。
  - 平均 FPS 高但 **1% Low 很低** = 偶发卡顿（掉帧）；**帧时间抖动大** = 微卡顿（画面不顺滑）。手感差就看这俩。
- **前台聚焦程序同步带 / 卡顿时刻标注（FT≥50ms）**：哪个程序在前台、什么时刻发生了明显卡顿（一帧超过 50 毫秒 = 肉眼可见的顿）。
- **前台 CPU/GPU 瓶颈指示**：卡的时候到底是 **CPU 拖后腿**还是 **GPU 拖后腿**，这条会告诉你（决定你该升级哪个件）。
- **全屏 vs 窗口流畅度关联**：对比你全屏玩和窗口玩，哪个更顺。

### 🔌 3.3 功耗与电费诊断舱（默认看最近 1 小时）
**回答**：这台电脑多费电？一个月电费多少？哪个程序最耗电？

- 顶部可以**自己改电价**：默认 **峰电 0.58 元/度（08:00–22:00）、谷电 0.27 元/度（22:00–08:00）**——按你当地实际电价下拉调整即可，电费就会自动重算。
- **今日累计能耗 / 累计电费与月底预测**：今天用了多少度、花了多少钱、按当前节奏**这个月底大概要花多少**。
- **实时整机总功耗时序堆叠图**：CPU + GPU + 外设的实时功耗叠加，看尖峰在哪。
- **高功耗应用 Top 10 黑榜**：哪些程序最费电（已按峰谷电价折算成钱），抓「电老虎」。
- **每日能耗趋势与游戏能效比 (FPS/W)**：每度电能换多少帧——数字越高越省电高效。
- **碳排放与种树足迹**：用电折算成 CO₂（系数 0.581 kg/度）和「相当于种几棵树」，纯科普。
- **PSU 各轨电流 / 电源负载率**：估算你的电源（按 1000W 算）负载率。**50% 左右是电源效率最佳区**，长期 >80% 说明电源吃紧。

### 🔍 3.4 进程取证与安全审计舱（默认看最近 7 天）
**回答**：有没有可疑程序、病毒、伪装的系统进程、偷偷外联的程序？

> 这是「安全摄像头」。**大部分表格平时是空的——空 = 没发现威胁 = 好事**，别慌。

- **🔴 高危仿冒组件数 / ⚠️ 未签名进程数 / 🔑 提权进程**：顶部几个数字卡。
  - **数字签名**：正规软件都有微软/厂商的「数字签名」。**❌ 无签名** 的程序要多看一眼（不一定是病毒，但可疑）。
  - **提权（is_elevated）**：以管理员权限运行的程序。
- **🎭 系统核心进程仿冒审计**：病毒爱伪装成 `svchost.exe`、`lsass.exe` 这类系统进程。这张表专抓**「名字像系统进程、但不在 `C:\Windows` 目录里」**的冒牌货——🔥 红色 = 严重可疑。
- **🛡️ LOLBins 滥用与高危脚本审计**：抓 `powershell`、`certutil`、`mshta` 等系统自带工具被拿来**下载东西/执行可疑命令**（黑客常用手法）。
- **🌐 静默网络外联 / 高危外联端口**：抓**没有窗口、却在偷偷联网**的程序，以及连接到 SSH(22)/远程桌面(3389)/数据库等敏感端口的连接。
- **👿 崩溃进程审计（退出码翻译）**：哪些程序崩溃了，并把退出码翻译成人话（如「🔥 内存越界访问」「栈溢出」）。
- **🌳 进程家谱**：命令行（cmd/powershell）孵化了哪些子进程——排查可疑脚本链。

### 🐀 3.5 资源大户与后台内鬼（默认看最近 1 小时）
**回答**：谁在偷偷吃我的 CPU / 内存 / 显存 / 硬盘 / 网络？

- **🏆 全进程 CPU/GPU/内存/显存 Top 25**：当前最占资源的程序排行榜。
- **👿 后台内鬼榜（静默高占用）**：**没在前台、却在大量占资源**的程序——最值得怀疑的「内鬼」。
- **💾 磁盘 I/O 与 SSD 写入损耗大户**：谁在拼命读写硬盘（频繁写入会折寿固态）。
- **🌐 网络流量大户 + 远端 IP 追踪**：谁在上传/下载，连到了哪个 IP。
- **🧵 线程/亲和性/共享显存细览**：进阶信息（开了多少线程、绑在哪些核上）。
- **🔴 无响应卡死进程时间线**：哪个程序「未响应」卡死了（红色），何时恢复。
- **🧪 内存/显存泄漏嫌疑榜**：通过「程序运行期间内存只涨不跌」来发现可能**内存泄漏**的程序。

### 📊 3.6 屏幕使用时间（默认看今年）
**回答**：我每天用电脑多久？都在用什么程序？专注还是频繁分心？

- **开机总时长（已剔除睡眠）/ 键鼠活跃总时间**：真实使用时长（睡眠时间不算）。
- **前台独占工时分布 / 标题排行 / 应用分类时间占比**：你的时间都花在哪些程序、哪些任务上。
- **专注时间**：连续待在同一个程序 ≥5 分钟算一段「心流」，看你能专注多久。
- **「频繁闪切」干扰指数**：你在不同程序间反复横跳的频率——越高说明越分心。
- **每日活跃时段热力图 / 周度对比**：哪个时段最活跃，本周比上周更努力还是更摸鱼。

---

## 4. 数据库到底记了什么

数据库名 `time_audit`，一共 6 张表（看不懂可跳过，知道「啥都记了」即可）：

| 表名 | 通俗解释 | 多久记一次 |
|---|---|---|
| `fact_system_hardware` | **整机体检表**：CPU/GPU 的温度·功率·频率·电压、内存、磁盘延迟、网络、游戏帧率 | 每 ~3 秒 1 行 |
| `fact_process_activity` | **每个程序的资源账单**：CPU/GPU/内存/显存/磁盘/网络/连接数/线程/是否卡死 | 每 ~3 秒，每个活跃程序 1 行 |
| `fact_process_context` | **前台窗口日记**：此刻哪个程序在前台、窗口标题、全屏还是窗口、停留多久 | 每次切窗口记 1 行 |
| `fact_process_lifecycle_events` | **进程生死簿**：程序何时启动、何时退出、活了多久、退出码 | 每次开/关程序 |
| `dim_process_registry` | **程序档案库**：程序路径、父进程、命令行、是否提权、有无数字签名、绑定的服务 | 首次见到该程序时 |
| `app_usage_logs` | **使用时长流水**（由 AHK 记录器写入）：用了哪个程序、窗口标题、起止、时长 | 每段使用 |

> 所有时间都按**北京时间（UTC+8）**对齐；数据按周/月「分箱」存储，长期不卡。
> 数据**只存在你自己这台电脑的数据库里，不上传任何云端**。

---

## 5. 诊断速查：电脑出问题了，看哪张盘？

| 你遇到的现象 | 先看这张盘 | 重点看什么 / 怎么判断 |
|---|---|---|
| 🎮 **游戏卡顿、不跟手** | 前台交互盘 + 硬件盘 | 1% Low 是否远低于平均 FPS、帧时间抖动是否大；同时看 GPU/CPU 温度是否撞墙、看「瓶颈指示」是 CPU 还是 GPU 限制 |
| 🐌 **电脑整体变慢** | 硬件盘 + 资源大户盘 | 内存占用是否 >90%、硬缺页是否飙高、磁盘延迟是否持续 >20ms；再去资源大户盘抓「后台内鬼」 |
| 🔥 **风扇狂转、机箱烫** | 硬件盘 | CPU/GPU 温度曲线、降频状态条是否变红（温度墙）；看老化趋势确认是否该清灰 |
| 💸 **电费太高 / 想省电** | 功耗盘 | 高功耗应用 Top10 抓电老虎；用峰谷电价看是否能把重活挪到谷电时段（22 点后）跑 |
| 🦠 **怀疑中毒/有可疑程序** | 安全审计盘 | 看「仿冒系统进程」「未签名进程」「静默外联」「LOLBins 滥用」几张表有没有红色行 |
| 📉 **某程序老崩溃** | 安全审计盘 | 「崩溃进程审计表」看退出码翻译，判断是内存越界还是被强杀 |
| ⏰ **想知道自己每天干了啥** | 屏幕使用时间盘 | 应用分类占比、专注时间、热力图 |
| 🕵️ **谁在偷偷联网/占资源** | 资源大户盘 + 安全盘 | 后台内鬼榜 + 网络流量大户 + 静默外联表 |
| 🧓 **硬件是不是老化了** | 硬件盘 | CPU/GPU「散热老化与能效衰减趋势」：同负载下温度逐月升高 = 散热老化 |

**通用排查口诀**：先用右上角时间范围**框到出问题的那个时间点**，再在硬件盘看「那一刻」CPU/GPU/内存/磁盘哪个指标异常，最后去资源大户盘看「那一刻」是哪个程序在作妖。

---

## 6.「正常 vs 异常」数字对照表

> 经验值，供小白快速判断；不同负载下会浮动，趋势比绝对值更重要。

| 指标 | 😀 正常 | 😐 偏高/注意 | 😱 异常/要处理 |
|---|---|---|---|
| CPU 温度（9950X3D） | 待机 40–55℃ / 负载 70–85℃ | 85–90℃ | **>90℃ 撞温度墙**（清灰、换硅脂） |
| GPU 核心温度（RTX5080） | 负载 60–78℃ | 78–83℃ | **>85℃** |
| GPU 热点温度 | 比核心高 10–15℃ | 高 15–20℃ | **>95℃**（散热/显存隐患） |
| 物理内存占用 | <70% | 70–90% | **>90% 且硬缺页飙高** |
| 系统提交大小 | < 物理内存(64GB) | 接近 64GB | **> 64GB**（在用页面文件，变慢） |
| 磁盘最高延迟 | <5ms | 偶发尖刺到 10–20ms | **持续 >20ms** |
| 网络 Ping | <30ms 且平稳 | 偶发跳高 | **丢包 >0 / 抖动剧烈** |
| GPU 降频状态 | 🟢 满血 / 🔵 空闲降频 | 🟠 功耗墙 | 🔴 **温度墙 / 🛑 硬件降速** |
| 游戏 1% Low | 接近平均 FPS | 比平均低 20–30% | **比平均低一半以上**（明显卡顿） |
| 电源负载率（1000W） | 30–60%（效率最佳） | 60–80% | **>80%**（电源吃紧） |
| 数字签名 | ✅ 已签名 | ⚠️ 签名异常 | **❌ 无签名 + 在用户目录运行** |

---

## 7. 日常维护、备份、自检

> 正常情况下**全自动，你什么都不用做**。下面是「想自己确认/出问题时」用得上的。

### 启动 / 停止
- **开机自启**：计划任务 `TimeAudit_AutoStart` 会自动拉起采集器 + Docker 容器，**开机即采集，无需手动**。
- **手动启动**（万一没自启）：用管理员身份运行 `E:\TimeAudit\start_all.bat`。
- **最省心的恢复方式**：遇到采集像是停了/数据不更新，**重启电脑**即可（一切会自动恢复）。

### 一键自检（推荐！判断「系统还在不在好好工作」）
打开终端（PowerShell），运行：
```
python E:\TimeAudit\test_telemetry_health.py     # 采集链路健康总检（组件存活/入库/分区/数据质量）
python E:\TimeAudit\test_optimizations.py        # 优化与修复回归（连接池/数据库调优/无响应检测等）
```
全部显示 **✅ PASS / 🎉 全部通过** 就说明系统健康。哪项 ❌ 就按它的提示排查。

### 怎么判断「采集器还在跑」
- 任务管理器里能找到 `python.exe` 或 `pythonw.exe`（命令行含 `main.py`）。
- 任意仪表盘把时间设成「最近 5 分钟」，曲线在持续更新 = 在采集。

### 备份
- 仪表盘配置会**每天自动备份**（计划任务 `TimeAudit_DailyBackup`）。
- 手动备份/恢复仪表盘：`python backup_grafana.py` / `python restore_grafana.py`。

### 数据会不会爆盘？
- 数据库按周/月自动分箱，日志文件超 50MB 自动截断，外部监视器日志有上限——**长期运行不会无声膨胀**。

---

## 8. 名词小词典

| 名词 | 大白话 |
|---|---|
| **采集器 / 探针** | 在后台悄悄记录电脑数据的程序 |
| **仪表盘（Dashboard）** | 把数据画成图表的网页报表 |
| **FPS / 帧率** | 每秒画几张画面，越高越流畅（60/144/240…） |
| **1% Low** | 最差那 1% 时刻的帧率，反映「偶发卡顿」 |
| **帧时间 / 抖动** | 画每一帧花的毫秒数；忽长忽短=画面不顺（微卡顿） |
| **温度墙 / 功耗墙** | 太热或到功耗上限时，硬件**主动降速**保护自己 |
| **CCD** | 9950X3D 内部的两块核心区（CCD0 带大缓存擅长游戏，CCD1 高频擅长多线程） |
| **热点温度（Hotspot）** | 芯片上最热的那个点，通常比「核心温度」更高 |
| **提交大小（Commit）** | 系统答应给程序的内存总量；超过物理内存就得动用硬盘（变慢） |
| **硬缺页** | 内存不够，被迫去硬盘搬数据，越多越卡 |
| **DPC / 调度抖动** | 系统被打断、不流畅的程度（本系统记的是用户态调度抖动，越低越顺） |
| **数字签名** | 软件的「身份证」，正规软件都有；无签名的要多留心 |
| **提权（Elevated）** | 以管理员权限运行 |
| **LOLBins** | 系统自带的正常工具被黑客「借刀杀人」用来干坏事 |
| **峰电 / 谷电** | 用电高峰时段电价贵（峰）、深夜便宜（谷），错峰用电更省钱 |
| **PSU** | 电源（给整机供电的那个盒子） |

---

> 📁 文件位置：`E:\TimeAudit\` ｜ 仪表盘：`http://localhost:53000`（admin/admin）
> 有疑问时第一步永远是：**跑一次 `test_telemetry_health.py` 看系统健不健康，再用右上角时间范围框到问题时刻去对照看图。**
